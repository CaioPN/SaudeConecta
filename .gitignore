# Dependências (geradas por `npm install` — nunca versionar)
node_modules/

# Variáveis de ambiente / segredos
.env
*.env
!*.env.example

# Chave da API de IA do chatbot (use o config.properties.example como modelo)
backend-api/config.properties

# Builds / saídas geradas
dist/
build/

# Java compilado
out/
*.class
backend-api/sources.txt

# Logs
*.log
npm-debug.log*

# Arquivos do sistema operacional / editores
.DS_Store
Thumbs.db
.vscode/
*.swp
